// Let's grad the hero div
const hero = document.querySelector(`hero`);

// let's modify the background
hero.style.backgroupImage = `url('./assets/sf.jpg')`;

const name = document.querySelector(`.hero h1 span`);
name.innerHTML = `Your name`;

const greeting = document.querySelector(`.hero h3`);
greeting.innerHTML = `Something really clever...`;
const profilePic = querySelector(`.profile-img`);
